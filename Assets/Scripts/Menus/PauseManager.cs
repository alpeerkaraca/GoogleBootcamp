using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;



public class PauseManager: MonoBehaviour 
{  /* 

    [Tooltip("Toggle component for sounds")]
    public Toggle soundsToggle;
    [Tooltip("Toggle component for music")]
    public Toggle musicToggle;

   void Start()
    {

        soundsToggle.onValueChanged.AddListener(OnSoundsChanged);

        musicToggle.onValueChanged.AddListener(OnMusicChanged);

    }

  void OnSoundsChanged(bool newValue)
    {
        Button.mute = !newValue;
        Correct.mute = !newValue;
        Wrong.mute = !newValue;
    }

    void OnMusicChanged(bool newValue)
    {
        Music.mute = !newValue; ;
    }
*/
}